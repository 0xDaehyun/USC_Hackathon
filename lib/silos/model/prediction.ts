import predictionJson from "@/data/model/eaton-aft-prediction.json";
import type { Feature, Polygon } from "geojson";
import type { FirePrediction } from "@/lib/silos/types";

/**
 * Generated by the ML repository's `pipeline:export-frontend-prediction`.
 * Contract validation runs in `npm test`; this assertion keeps API consumers
 * on the same TypeScript shape without duplicating the generated values.
 */
export const EATON_AFT_PREDICTION =
  predictionJson as unknown as FirePrediction;

export const EATON_INITIALIZATION_BOUNDARY =
  EATON_AFT_PREDICTION.hourly_steps[0]
    .spread_polygon_geojson as Feature<Polygon>;
